{
  "name": "estatepay-functions",
  "description": "EstatePay Cloud Functions — production backend",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "lint": "echo \"(lint step placeholder — add eslint if desired)\" && exit 0",
    "serve": "firebase emulators:start --only functions,firestore,auth,hosting",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "axios": "^1.7.4",
    "firebase-admin": "^12.3.1",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
